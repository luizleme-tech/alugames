CREATE TABLE gamers(
                      id INT AUTO_INCREMENT PRIMARY KEY,
                      nome VARCHAR(255),
                      email VARCHAR(255),
                      dataNascimento VARCHAR(255),
                      usuario VARCHAR(255)
);

COMMIT;